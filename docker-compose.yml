services:
  sre-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sre-go-scratch
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped